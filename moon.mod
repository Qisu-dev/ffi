name = "Qisu-dev/ffi"

version = "0.1.0"

readme = "README.mbt.md"

repository = "https://github.com/Qisu-dev/ffi"

license = "Apache-2.0"

keywords = ["ffi", "native", "syscall", "sys", "libc"]

preferred_target = "native"

supported_targets = "native"

description = ""

source = "src"
